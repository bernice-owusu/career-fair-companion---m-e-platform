# Disable HMR + file watching. Used when auto-editing files to prevent Dev Server flickering/rebuild churn.
DISABLE_HMR=true

# Supabase project (shared with the main Nexus site) — anon key, RLS-scoped
# to INSERT-only on the `registrations` table. See src/services/supabaseClient.ts.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=